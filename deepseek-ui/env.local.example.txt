# =============================================================================
# Trading Bot — Environment Variables Reference
# =============================================================================
# Copy this file to .env.local and fill in your values:
#
#   cp env.local.example.txt .env.local
#
# Required variables are marked [REQUIRED].
# Everything else has a sensible default and is optional.
# =============================================================================


# -----------------------------------------------------------------------------
# DATABASE  [REQUIRED]
# -----------------------------------------------------------------------------
# PostgreSQL connection string for Prisma.
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://tradingbot:tradingbot123@localhost:5432/tradingdb


# -----------------------------------------------------------------------------
# INTERACTIVE BROKERS SERVICE
# URL of the running ib_service.py FastAPI server.
# Default: http://localhost:8765
# -----------------------------------------------------------------------------
IB_SERVICE_URL=http://localhost:8765


# -----------------------------------------------------------------------------
# AI / OLLAMA
# URL of the local Ollama instance used for AI sentiment analysis.
# Default: http://localhost:11434
#
# OLLAMA_MODEL: model to use for trading analysis.
#   Recommended: deepseek-r1:14b (high quality, slower)
#                deepseek-r1:7b  (faster, slightly less accurate)
# Valid: any model name available in your Ollama installation.
# -----------------------------------------------------------------------------
OLLAMA_API_URL=http://localhost:11434
OLLAMA_MODEL=deepseek-r1:14b


# -----------------------------------------------------------------------------
# WORLD MONITOR
# URL of the WorldMonitor geopolitical context service.
# Default: http://localhost:3000
# -----------------------------------------------------------------------------
WORLDMONITOR_URL=http://localhost:3000


# -----------------------------------------------------------------------------
# LOGGING
# Controls the minimum log level for the Next.js trading bot process.
# Valid: debug | info | warn | error      Default: info
#
# Use 'debug' during development to see all detail.
# Use 'warn' or 'error' in production to reduce log volume.
# -----------------------------------------------------------------------------
LOG_LEVEL=info


# -----------------------------------------------------------------------------
# NODE ENVIRONMENT
# Controls Prisma logging, structured JSON output, and other behaviours.
# Valid: development | production | test      Default: development
# -----------------------------------------------------------------------------
NODE_ENV=development


# ActivityLog retention period in days (default: 90). Rows older than this are deleted daily.
# ACTIVITY_LOG_RETENTION_DAYS=90

# Notification retention period in days (default: 90). Bell notification history older than
# this is deleted in the same nightly cleanup pass as ActivityLog.
# NOTIFICATION_RETENTION_DAYS=90

# Secret token for the /api/cron/cleanup endpoint.
# Generate one with: openssl rand -hex 32
# Pass it in the cron job: curl -H "X-Cron-Secret: <value>" http://localhost:3001/api/cron/cleanup
# Required in production. Optional in development (endpoint is open without it).
# CRON_SECRET=your-random-secret-here

# URL of the Next.js app itself (used by websocket-server and trading-engine for internal calls)
# Default: http://localhost:3001
# NEXTJS_URL=http://localhost:3001

# WebSocket server port (default: 3002)
# WS_PORT=3002

# Standalone trading bot control/status port (default: 3003)
# BOT_PORT=3003

# Optional explicit URL for standalone bot (overrides BOT_PORT)
# BOT_URL=http://localhost:3003

# WebSocket CORS allowed origins (comma-separated, default: localhost:3001,localhost:3000)
# WS_CORS_ORIGINS=http://localhost:3001,http://localhost:3000


# =============================================================================
# ib_service.py ENVIRONMENT VARIABLES (set in shell or systemd unit)
# =============================================================================

# IB TWS / Gateway connection
# IB_HOST=127.0.0.1
# IB_PORT=7497          # 7497=paper TWS | 7496=live TWS | 4002=paper GW | 4001=live GW
# IB_CLIENT=10          # clientId — avoid 1 (often reserved by TWS)
# IB_SERVICE_PORT=8765  # Port that ib_service.py listens on

# Optional API key auth for ib_service.py (leave unset for localhost-only)
# IB_API_KEY=

# Log level for ib_service.py
# Valid: DEBUG | INFO | WARNING | ERROR      Default: INFO
# IB_LOG_LEVEL=INFO

# Set to 1 to enable per-request access logging from uvicorn (noisy, for debugging)
# IB_ACCESS_LOG=0
